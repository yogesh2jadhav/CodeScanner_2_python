# Where the Vite dev server proxies /api requests (FastAPI backend)
VITE_API_PROXY_TARGET=http://localhost:8000
# Optional absolute API base URL for production builds served from another origin
VITE_API_BASE_URL=
